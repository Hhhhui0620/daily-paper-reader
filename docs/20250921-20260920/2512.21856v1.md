---
title: "Breaking Alignment Barriers: TPS-Driven Semantic Correlation Learning for Alignment-Free RGB-T Salient Object Detection"
authors: "Lupiao Hu, Fasheng Wang, Fangmei Chen, Fuming Sun, Haojie Li"
date: 2025-12-26
pdf: "https://arxiv.org/pdf/2512.21856v1"
tags: ["query:uav-rgbt-sod"]
score: 9
source: arxiv
selection_source: long-range
id: 2512.21856v1
canonical_id: "arxiv:2512.21856"
research_run_id: 20260921-e959361397be
research_mode: starter
reading_status: complete
title_zh: 打破对齐障碍：TPS驱动的语义关联学习用于无对齐RGB-T显著目标检测
tldr: 现有RGB-T显著目标检测方法依赖人工对齐标注数据，难以应对真实场景中空间错位、尺度变化和视角偏移等跨模态差异。本文提出TPS-SCL网络，以双流MobileViT为编码器并结合Mamba扫描机制，通过薄板样条对齐模块与语义相关性约束模块实现无对齐RGB-T检测。实验表明该方法在轻量级SOD方法中达到SOTA，并优于主流RGB-T方法。
motivation: 真实场景RGB-T图像对常存在空间错位与视角偏移，现有依赖人工对齐数据的方法性能急剧下降。
method: 提出TPS-SCL：双流MobileViT编码器加Mamba扫描，结合SCCM语义约束、TPSAM薄板样条对齐与CMCM跨模态关联模块。
result: 在多个数据集上达到轻量级SOD方法SOTA，并超越主流RGB-T显著目标检测方法。
conclusion: 为无对齐RGB-T显著目标检测提供高效方案，兼顾低参数量与跨模态相关性建模能力。
evidence: 面向UAV-RGBT显著目标检测，解决无对齐跨模态图像的空间错位与语义关联问题。
reading_content_version: 1
reading_section: deep
---

## 摘要
现有的RGB-T显著目标检测方法主要依赖于手动对齐和标注的数据集，难以处理具有原始、未对齐RGB-T图像对的真实场景。在实际应用中，由于空间错位、尺度变化和视角偏移等显著的跨模态差异，当前方法在未对齐数据集上的性能急剧下降。为了解决这个问题，我们提出了一种针对真实世界未对齐图像对的高效RGB-T SOD方法，称为薄板样条驱动的语义关联学习网络（TPS-SCL）。我们采用双流MobileViT作为编码器，结合高效的Mamba扫描机制，在保持低参数数量和计算开销的同时，有效建模两种模态之间的关联。为了在对齐过程中抑制冗余背景信息的干扰，我们设计了一个语义关联约束模块（SCCM）来分层约束显著特征。此外，我们引入了一个薄板样条对齐模块（TPSAM）来减轻模态之间的空间差异。另外，还加入了一个跨模态关联模块（CMCM）来充分探索和整合模态间的依赖关系，从而提升检测性能。在各种数据集上的大量实验表明，TPS-SCL在现有轻量级SOD方法中达到了最先进的性能，并优于主流的RGB-T SOD方法。

## Abstract
Existing RGB-T salient object detection methods predominantly rely on manually aligned and annotated datasets, struggling to handle real-world scenarios with raw, unaligned RGB-T image pairs. In practical applications, due to significant cross-modal disparities such as spatial misalignment, scale variations, and viewpoint shifts, the performance of current methods drastically deteriorates on unaligned datasets. To address this issue, we propose an efficient RGB-T SOD method for real-world unaligned image pairs, termed Thin-Plate Spline-driven Semantic Correlation Learning Network (TPS-SCL). We employ a dual-stream MobileViT as the encoder, combined with efficient Mamba scanning mechanisms, to effectively model correlations between the two modalities while maintaining low parameter counts and computational overhead. To suppress interference from redundant background information during alignment, we design a Semantic Correlation Constraint Module (SCCM) to hierarchically constrain salient features. Furthermore, we introduce a Thin-Plate Spline Alignment Module (TPSAM) to mitigate spatial discrepancies between modalities. Additionally, a Cross-Modal Correlation Module (CMCM) is incorporated to fully explore and integrate inter-modal dependencies, enhancing detection performance. Extensive experiments on various datasets demonstrate that TPS-SCL attains state-of-the-art (SOTA) performance among existing lightweight SOD methods and outperforms mainstream RGB-T SOD approaches.

## 专题评审

专题相关性评分：9/10。

该文直接针对未配准RGB-T显著目标检测提出跨模态对齐方法，与专题核心任务高度一致。

---

## 论文详细总结（自动生成）

# TPS-SCL 论文深度总结

## 1. 核心问题与整体含义

- **研究动机**：现有 RGB-T 显著目标检测（SOD）方法几乎全部依赖人工对齐和标注的数据集进行训练与评测。然而，真实场景中由传感器直接采集的 RGB-T 图像对天然存在**空间错位、尺度变化和视角偏移**等跨模态差异，导致现有方法在未对齐数据上性能急剧下降。
- **核心问题**：如何在不依赖人工对齐的前提下，有效建模 RGB 与热红外模态之间的语义关联，实现鲁棒且高效的 RGB-T 显著目标检测。
- **整体含义**：该工作旨在打破“必须对齐才能检测”的范式壁垒，推动 RGB-T SOD 从实验室对齐数据走向真实未对齐场景，对 UAV 等实际部署场景具有直接意义。

## 2. 方法论

### 2.1 核心思想
提出 **TPS-SCL**（Thin-Plate Spline-driven Semantic Correlation Learning Network），以“先语义约束、再显式对齐、后深度关联”的三阶段策略处理未对齐 RGB-T 图像对。

### 2.2 整体架构
- **编码器**：双流 MobileViT-S，分别提取 RGB 与热红外的四级多层级特征。
- **核心模块**：SCCM（语义关联约束）、TPSAM（薄板样条对齐）、CMCM（跨模态关联）。
- **解码器**：自顶向下融合多级特征，输出最终显著图。
- **扫描机制**：基于 Efficient VMamba（ES2D）与 LocalMamba（LSSM），以线性复杂度建模长程依赖。

### 2.3 关键技术细节

**（1）SCCM 语义关联约束模块**
- 输入最高层语义特征，经差分增强模块（DEM）保留模态特有信息。
- 通过深度可分离卷积、线性投影、层归一化生成共享特征 H，再经 ES2D 捕获跨模态长程空间依赖。
- 引入轻量级 SGE 注意力抑制冗余通道信息，生成**显著引导特征（SGF）**。
- SGF 上采样后逐层约束浅层特征，使浅层聚焦共显著区域、抑制背景噪声。

**（2）TPSAM 薄板样条对齐模块**
- 先经 LSSM 局部窗口扫描增强局部结构与边界细节，再用 SGE 抑制冗余。
- 拼接双模态特征后经 GAP 与全连接层预测控制点位移 (Δx, Δy)，动态更新目标控制点坐标 Q。
- 构造距离矩阵 K（基于 RBF：r²log r）与增广矩阵 L，通过伪逆求解变换参数 W = L†Y。
- 生成平滑映射函数 G，将热红外显著区域**非线性形变**至 RGB 坐标空间，得到对齐后的热特征 Aᵢₜ。
- 相比单应性估计，TPS 能处理局部非线性形变，更适应大视角变化。

**（3）CMCM 跨模态关联模块**
- 将对齐后的热特征与增强 RGB 特征经 DEM 增强后，投影至共享隐状态空间，得到 y_rgb 与 y_t。
- 生成门控参数 Z_rgb、Z_t，通过门控机制在隐状态空间进行跨模态调制融合。
- 经 SGE 抑制冗余并残差连接保留原始信息，输出强相关特征。
- 最终拼接融合后送入解码器，以 BCE + 平滑损失 + Dice 损失联合优化。

## 3. 实验设计

### 3.1 数据集与场景
- **训练集**：UVT20K 训练集（未对齐）；VT5000 训练集（对齐版本）。
- **测试集（未对齐/弱对齐）**：UVT20K、UVT2000、un-VT821、un-VT1000、un-VT5000。
- **测试集（对齐）**：VT821、VT1000、VT5000。
- **评测指标**：E-measure（Em）、S-measure（Sm）、F-measure（Fm）。

### 3.2 对比方法
- **重量级方法（10 种）**：DCNet、LAFB、MSEDNet、ConTriNet、WaveNet、SPNet、SwinNet、TCINet、SACNet、PCNet。
- **轻量级方法（6 种）**：MoADNet、MobileSal、OSRNet、LSNet、HENet、LGPNet。
- 共对比 **16 种 SOTA 方法**，涵盖 VGG16、ResNet、SwinB、PVT-v2、MobileNet 等多种骨干网络。

### 3.3 Benchmark 设置
- 轻量级版本以 MobileNet-S 为骨干，参数量 12.82M、FLOPs 12.34G。
- 重量级版本替换为 SwinB 与 PVT-v2-B4 骨干，验证模块的骨干适应性。

## 4. 资源与算力

- **GPU**：单张 RTX 4090。
- **优化器**：AdamW，学习率 1e-5，权重衰减 1e-4。
- **训练配置**：batch size 为 4，训练 200 个 epoch，输入分辨率 384×384。
- **未明确说明**：论文未报告具体训练时长、GPU 总消耗量或能耗数据。

## 5. 实验数量与充分性

- **实验组数概览**：
  - 主对比实验：2 张表（重量级 + 轻量级），覆盖 8 个数据集 × 3 个指标。
  - 消融实验：3 组（w/o SCCM、w/o TPSAM、w/o CMCM），在 UVT20K 与 UVT2000 上验证。
  - 骨干适配实验：MobileViT-S、SwinB、PVT-v2-B4 三种骨干。
  - 可视化分析：TPSAM 与 CMCM 的特征可视化对比。
- **充分性评价**：
  - 数据集覆盖全面，兼顾未对齐、弱对齐与对齐三类场景，跨 8 个数据集验证泛化性。
  - 消融实验直接对应三大核心模块，逻辑清晰。
  - 对比方法数量充足（16 种），且包含重量级与轻量级两类，比较维度合理。
- **公平性**：作者声明对比方法结果或来自原文报告，或使用公开代码默认参数运行，具备一定公平性；但未详细说明是否对所有方法进行统一超参调优。

## 6. 主要结论与发现

- TPS-SCL 在**轻量级 SOD 方法中达到 SOTA**，参数量仅 12.82M、FLOPs 12.34G。
- 相比次优轻量级方法 HENet，在 UVT20K 上 Fm 提升 **+7.0%**、Sm 提升 **+1.5%**、Em 提升 **+2.9%**；在 UVT2000 上 Fm 提升 **+5.0%**。
- 重量级版本（SwinB 骨干）在 8 个数据集上均表现极具竞争力，相比 PCNet 在 UVT20K 上 Fm 提升 **+2.1%**。
- 消融实验证实：**SCCM 贡献最大**（移除后 UVT20K 上 Fm 下降 79.3%），TPSAM 与 CMCM 均对性能有显著正向贡献。
- 可视化表明，经 SCCM + TPSAM + CMCM 处理后，双模态特征空间差异显著减小，背景噪声被有效抑制。

## 7. 优点

- **方法创新性**：首次将薄板样条（TPS）变换引入无对齐 RGB-T SOD，能处理单应性估计无法应对的局部非线性形变。
- **模块协同设计**：SCCM → TPSAM → CMCM 形成“语义约束—显式对齐—深度关联”的递进式流程，逻辑自洽。
- **效率与性能平衡**：基于 MobileViT + Mamba 扫描机制，在低参数量与低计算量下达到 SOTA，适合实际部署。
- **骨干泛化性**：在 MobileViT-S、SwinB、PVT-v2-B4 三种骨干上均验证有效，模块即插即用性强。
- **实验覆盖全面**：8 个数据集、16 种对比方法、3 组消融，验证维度丰富。
- **代码开源**：提供 GitHub 链接，便于复现与后续研究。

## 8. 不足与局限

- **算力报告不完整**：未说明训练总时长、GPU 小时数或能耗，不利于资源评估与复现成本预估。
- **消融实验覆盖有限**：仅在 UVT20K 与 UVT2000 两个未对齐数据集上做消融，未在弱对齐/对齐数据集上验证模块必要性。
- **控制点预测依赖全连接层**：TPSAM 中控制点位移由 FC 层预测，其数量与初始化策略未详细讨论，可能对极端形变场景敏感。
- **未讨论失败案例**：论文未展示在极端视角变化或模态严重缺失（如热红外失效）下的失败案例，鲁棒性边界不清晰。
- **公平性细节不足**：对比方法是否统一调参、是否使用相同训练策略未完全说明，存在潜在比较偏差风险。
- **应用限制**：方法针对 RGB-T 双模态设计，扩展至其他模态组合（如 RGB-D、RGB-LiDAR）需额外验证。
- **实时性未评估**：虽强调轻量级，但未报告推理速度（FPS），对实时 UAV 部署的适用性缺乏直接证据。

（完）
